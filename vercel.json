{
	"installCommand": "cd frontend && npm install",
	"buildCommand": "cd frontend && npm run build:artifact",
	"outputDirectory": "frontend/.output",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
