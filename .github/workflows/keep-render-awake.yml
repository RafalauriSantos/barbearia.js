name: Keep Render backend awake

on:
  schedule:
    - cron: "*/13 * * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: keep-render-backend-awake
  cancel-in-progress: false

jobs:
  ping:
    name: Ping backend health
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Call Render health endpoint
        run: |
          curl --fail --show-error --silent --max-time 45 --retry 1 --retry-delay 10 \
            "https://kurt-api.onrender.com/health"
